{
  "name": "magical-predicate-builder",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prop-types": "^15.6.2",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-icons": "^3.2.2",
    "react-scripts": "2.1.1",
    "styled-components": "^4.1.2"
  },
  "devDependencies": {
    "gh-pages": "latest",
    "webpack-bundle-analyzer": "latest"
  },
  "scripts": {
    "analyze": "npm run build -- --stats && webpack-bundle-analyzer build/bundle-stats.json",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
